Algoritmo "tabuada"


Var
    x, y, tabuada : inteiro

Inicio

  escreva("Deseja a tabuada para qual valor: ")
  leia (tabuada)
      para x de 1 ate 10 faca
       escreva(tabuada)
        y <- tabuada * x
        escreval (" x ",  x ," = ",  y)
        x <- y + tabuada

      fimpara
  escreval ("Bons estudos!")

Fimalgoritmo